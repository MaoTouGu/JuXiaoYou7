<ForestPage x:Class="MaoTouGu.JuXiaoYou.Indexing.FilterView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:ui="urn:KinonekoSoftware/ui"
            xmlns:this="clr-namespace:MaoTouGu.JuXiaoYou.Indexing"
            xmlns:thisVM="clr-namespace:MaoTouGu.JuXiaoYou.Indexing"
            xmlns:objects="clr-namespace:MaoTouGu.Studio.Database.Objects;assembly=MaoTouGu.Studio.Database"
            mc:Ignorable="d"
            d:DesignWidth="800" d:DesignHeight="450"
            d:DataContext="{d:DesignInstance Type={x:Type thisVM:FilterViewModel}, IsDesignTimeCreatable=True}"
            Padding="0 8">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type objects:Moniker}">
            <this:WithStarPresenter/>
        </DataTemplate>

        <Style TargetType="{x:Type ui:ListBoxItem}" BasedOn="{StaticResource ListBoxItem.Outline}">
            <Setter Property="Margin" Value="3" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0 0 0 4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ui:ListBoxItem}">
                        <Border x:Name="PART_Border"
                                    Padding="{TemplateBinding Padding}"
                                    Background="{TemplateBinding Background}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="Transparent"
                                    CornerRadius="0">
                            <ContentPresenter x:Name="PART_Content"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                                  ContentStringFormat="{TemplateBinding ContentStringFormat}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True" />
                                    <Condition Property="IsSelected" Value="False" />
                                </MultiTrigger.Conditions>
                                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Overlay100}" TargetName="PART_Border" />
                            </MultiTrigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource Brush.HighlightB4}" TargetName="PART_Border" />
                                <Setter Property="Background" Value="#20000000" TargetName="PART_Border"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="TextElement.Foreground" Value="{DynamicResource Brush.Foreground.Disabled}" TargetName="PART_Content" />
                                <Setter Property="BorderBrush" Value="{DynamicResource Brush.HighlightC1}" TargetName="PART_Border" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Margin="0 0 16 8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"
                        Orientation="Horizontal">
                <ui:MainButton ui:ControlAssist.CornerRadius="0"
                               Content="新建"
                               Command="{Binding Add}"
                               Margin="3 0"
                               Padding="32 8"
                               Width="160"/>

            </StackPanel>

            <StackPanel Grid.Column="2"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">

                <ui:MainButton ui:ControlAssist.CornerRadius="0"
                               Content="编辑"
                               Command="{Binding Edit}"
                               CommandParameter="{Binding Moniker, Mode=OneWay}"
                               Margin="3 0"
                               Padding="32 8"/>
                <ui:GhostButton ui:ControlAssist.CornerRadius="0"
                                Content="设卡"
                                Command="{Binding TemplateVisualize}"
                                CommandParameter="{Binding Moniker, Mode=OneWay}"
                                Margin="3 0"
                                Padding="32 8"/>
                <ui:GhostButton ui:ControlAssist.CornerRadius="0"
                               Content="删除"
                               Command="{Binding Remove}"
                               CommandParameter="{Binding Moniker, Mode=OneWay}"
                               Margin="3 0"
                               Padding="32 8"/>
                <ui:GhostButton ui:ControlAssist.CornerRadius="0"
                                 ui:ControlAssist.Icon="{StaticResource More}"
                                ui:ControlAssist.IsFilled="True"
                                ContextMenuAssist.UseClick="True"
                               Content="分享"
                                IconMode="Icon"
                               Margin="3 0">
                    <ui:GhostButton.ContextMenu>
                        <ui:ContextMenu Placement="Bottom">
                            <ui:MenuItem Header="Quick"
                                         Click="Menu_Quick"/>
                            <ui:MenuItem Header="添加设定"
                                         Click="Menu_AddSetting"/>
                            <ui:MenuItem Header="添加头像"
                                         Click="Menu_AddGravatar"/>
                        </ui:ContextMenu>
                    </ui:GhostButton.ContextMenu>
                </ui:GhostButton>
            </StackPanel>
        </Grid>
        
        <ui:ListBox Grid.Row="1"
                    ItemsSource="{Binding Monikers}"
                    SelectedItem="{Binding Moniker, Mode=TwoWay}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ui:ListBox>
    </Grid>
</ForestPage>