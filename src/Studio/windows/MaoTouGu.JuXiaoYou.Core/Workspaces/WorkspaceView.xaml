<ForestPage x:Class="MaoTouGu.JuXiaoYou.Workspaces.WorkspaceView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:ui="urn:KinonekoSoftware/ui"
            xmlns:dock="https://github.com/Dirkster99/AvalonDock"
            xmlns:this="clr-namespace:MaoTouGu.JuXiaoYou.Workspaces"
            xmlns:thisVM="clr-namespace:MaoTouGu.JuXiaoYou.Workspaces"
            xmlns:reskeys="clr-namespace:AvalonDock.Themes.VS2013.Themes;assembly=AvalonDock.Themes.VS2013"
            xmlns:thisM="clr-namespace:MaoTouGu.JuXiaoYou.Workspaces"
            xmlns:thisM2="clr-namespace:MaoTouGu.JuXiaoYou.Workspaces"
            xmlns:feature="clr-namespace:MaoTouGu.JuXiaoYou.Services.Plugins;assembly=MaoTouGu.JuXiaoYou.Services.Plugins"
            xmlns:dbCore="clr-namespace:MaoTouGu.Studio.Database.Objects;assembly=MaoTouGu.Studio.Database"
            xmlns:avalonDockProperties="clr-namespace:AvalonDock.Properties;assembly=AvalonDock"
            mc:Ignorable="d"
            d:DesignWidth="800" d:DesignHeight="450"
            d:DataContext="{d:DesignInstance Type={x:Type thisVM:WorkspaceViewModel}, IsDesignTimeCreatable=True}"
            Padding="8">
    <UserControl.Resources>
        <ResourceDictionary>

            <Style TargetType="{x:Type ui:TreeViewItem}">
                <Style.Triggers>
                    <Trigger Property="VirtualizingPanel.IsVirtualizing" Value="True">
                        <Setter Property="ItemsPanel">
                            <Setter.Value>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel />
                                </ItemsPanelTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <Setter Property="Padding" Value="0 6" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="IsTabStop" Value="True" />
                <Setter Property="OverridesDefaultStyle" Value="True" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="Foreground" Value="{DynamicResource Brush.Foreground.Level1}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ui:TreeViewItem}">
                            <AdornerDecorator>
                                <Grid Background="{TemplateBinding Background}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Border x:Name="PART_Border"
                                            Grid.Row="0"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="4 0 0 0"
                                            CornerRadius="0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" MinWidth="12" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <ToggleButton x:Name="Expander"
                                                          Grid.Column="0"
                                                          Margin="8 0"
                                                          ClickMode="Release"
                                                          Style="{StaticResource ClearStyle.TreeView.ToggleButton}"
                                                          IsChecked="{Binding IsExpanded , RelativeSource={RelativeSource TemplatedParent}}" />

                                            <ContentPresenter x:Name="PART_Content"
                                                              Grid.Column="2"
                                                              TextElement.FontSize="{TemplateBinding FontSize}"
                                                              Margin="{TemplateBinding Padding}"
                                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                              ContentSource="Header" />
                                        </Grid>
                                    </Border>
                                    <Grid Grid.Row="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" MinWidth="19" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <ItemsPresenter x:Name="ItemsHost"
                                                        Grid.Column="1"
                                                        Grid.ColumnSpan="2"
                                                        Visibility="Collapsed" />
                                    </Grid>
                                </Grid>
                            </AdornerDecorator>
                            <ControlTemplate.Triggers>
                                <Trigger Property="HasItems" Value="False">
                                    <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                                </Trigger>
                                <Trigger Property="IsExpanded" Value="True">
                                    <Setter TargetName="ItemsHost" Property="Visibility" Value="Visible" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#10FFFFFF" TargetName="PART_Border" />
                                    <Setter Property="BorderBrush" Value="#727920" TargetName="PART_Border" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="HasHeader" Value="False" />
                                        <Condition Property="Width" Value="Auto" />
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="PART_Content" Property="MinWidth" Value="75" />
                                </MultiTrigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="HasHeader" Value="False" />
                                        <Condition Property="Height" Value="Auto" />
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="PART_Content" Property="MinHeight" Value="19" />
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"
                              MinWidth="320"
                              MaxWidth="960" />
            <ColumnDefinition Width="4*" MinWidth="768" />
        </Grid.ColumnDefinitions>

        <this:ProjectSystemSideMenu Grid.Column="0"
                                    Margin="0 0 16 0" />

        <!--<GridSplitter Grid.Column="0"
                      Background="{DynamicResource Brush.Foreground.Level3}"
                      BorderBrush="Transparent"
                      BorderThickness="8"
                      Width="17"/>-->
        <DockPanel Grid.Column="1">
            <ui:TabbedListBox DockPanel.Dock="Top"
                              HorizontalAlignment="Left"
                              ItemsSource="{Binding Tabs, Mode=OneWay}"
                              SelectedItem="{Binding Tab, Mode=TwoWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <CloseButton DockPanel.Dock="Right"
                                         Click="Button_CloseTab" />

                            <TextBlock Margin="4 0"
                                       Text="{Binding FriendlyTitle, Mode=OneWay}"
                                       VerticalAlignment="Center" />
                        </DockPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ui:TabbedListBox>

            <ContentHost IsInfrastructure="False"
                         ViewModel="{Binding Tab,Mode=OneWay}">

            </ContentHost>
        </DockPanel>
    </Grid>
</ForestPage>